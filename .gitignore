# Local database config with passwords (root only — NOT includes/)
/config.php

# Full database dump (contains real data) — use schema-only file instead
timetable_db.sql

# OS files
.DS_Store
Thumbs.db

# IDE files
.idea/
.vscode/

# Claude Code local config, agents, and session state
.claude/

# Browser test artifacts
.playwright-mcp/

# Agent-generated handoff/scratch docs — session notes, not project docs
HANDOFF.md
CLAUDE_HANDOFF.md
FACULTY_COURSE_COMBINATIONS.md
*_HANDOFF.md
*.original.md

# Source workbook and its import — real staff names and the department's timetable
*.xls
*.xlsx
/data/imported_timetables.json
